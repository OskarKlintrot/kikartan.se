@page "/slider"

<h1>Slider</h1>

<p>Current value: @(currentCount / 10M)</p>

<input type="range" min="8" max="30" step="1" @bind-value="@currentCount" @bind-value:event="oninput" />


@code {
    int currentCount = 8;
}
